[package]
name = "liminality-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
liminality-model = { path = "../liminality-model" }

[dev-dependencies]
serde_json = "1.0.150"
